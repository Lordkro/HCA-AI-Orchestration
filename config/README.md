# Configuration Directory

This directory contains configuration files for the HCA Orchestration system.

## Files

- `logging.yml` - Logging configuration (future)
- `models.yml` - Model configurations and parameters (future)

## Environment Variables

Configuration can also be done via environment variables. See `.env.example` in the project root.
